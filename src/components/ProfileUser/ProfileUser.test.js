import React from 'react';
import ReactDOM from 'react-dom';
import Profileuser from './';

it('renders without crashing', () => {
  const div = document.createElement('div');
  ReactDOM.render(<Profileuser />, div);
}); 
