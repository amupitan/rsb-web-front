import User from './';

//TODO: remove this stub test when User is completed. 
// It is here because a test file requires at least one test
test('user objects are created', () => {
    let testUser = new User();
    expect(testUser !== undefined).toBe(true);
  });