import dummy from '../../dummy/allUsers';

export default class User {
    
    //Get this to create routes for all the users
    getAllUsers(){
        //Get all users from backend
        return dummy;
    }
};