

import { getCurrentLocation } from './map';
import {
    getAddress,
    googleApiKey,
    googleApiVersion,
    loadGoogleMaps,
    removeGoogleMaps,
} from './google';

export {
    getAddress,
    getCurrentLocation,
    googleApiKey,
    googleApiVersion,
    loadGoogleMaps,
    removeGoogleMaps,
};